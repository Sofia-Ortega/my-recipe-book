//search bar to look through recpiecards
import React from 'react'

export default function SearchBar() {
  const searchBarStyle = {
    flex: 12,
    textAlign: "center",
  }

  return(
    <div style={searchBarStyle}>
      Search Bar
    </div>
  )
}
